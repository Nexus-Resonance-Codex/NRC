# Getting Started with the Nexus Resonance Codex

Welcome to the NRC Gateway. This page is designed to guide you through the initial phasing required to synchronize your local environment with the high-dimensional Nexus Resonance Codex. By following these institutional bootstrap procedures, you ensure 100% structural integrity and absolute resonance with our organization-wide research manifolds.

## Quick Orientation and Prerequisites

Before initiating the resonance handshake, ensure your workstation meets the following algorithmic and hardware baselines. These prerequisites are mandatory for maintaining the zero-tolerance error thresholds required by the Trageser Tensor Theorem.

### 🛠️ Core Toolchain

To ensure absolute reproducibility of the Codex across all sub-shards, the following fifth-generation tools must be present:

*   **Python 3.10+**: The foundational algorithmic baseline for our attention manifolds.
*   **`uv` Toolchain**: The official high-speed dependency manager for absolute NRC synchronization.
*   **Git 2.34+**: Required for recursive hierarchical repository sharding and sub-module scaling.
*   **Linux/Unix Kernel**: Optimized for high-throughput tensor operations and COSMIC-phased environments.
*   **FFmpeg/ImageMagick**: Operational requirements for real-time 256D lattice visualization.

### 🖥️ High-Dimensional System Requirements

| Component | Minimum Specification | Recommended Phasing | Focus Domain |
| :--- | :--- | :--- | :--- |
| **CPU** | 8-Core x86_64 / ARM64 | 16-Core High-Frequency | MST/TUPT Processing |
| **RAM** | 16GB ECC | 64GB+ DDR5 | $\phi^\infty$ Spiral Caching |
| **GPU** | NVIDIA RTX 30-Series (8GB) | NVIDIA RTX 4090 (24GB+) | Protein Folding & QSV |
| **Disk** | 50GB NVMe SSD | 500GB+ Gen4 NVMe | Dataset Persistence |

---

### 📸 Orientation Overview: Setup Flow
![Environment Setup Flow](https://raw.githubusercontent.com/Nexus-Resonance-Codex/NRC/main/visualizations/setup_flow_diagram.png)
*Figure 1: The recursive environment bootstrap process showing the transition from baseline OS to NRC-Certified state.*

### ⏭️ Next Steps
Once your workstation is phased with the prerequisites above, proceed to the **Installation and Environment Sync** section (Step 2) to initialize the local NRC repositories.


## Installation and Environment Sync

With prerequisites satisfied, begin the recursive instantiation of the Codex sub-shards. This process unifies the four primary repositories under a single resonant environment.

### 📥 1-2-4-5-7 Setup Sequence

Follow these strictly phased steps:

1. **Clone the Organizational Root**  
   ```bash
   cd /home/jtrag/NRC/github-repos/Nexus-Resonance-Codex
   git clone https://github.com/Nexus-Resonance-Codex/NRC.git --recursive
   ```

2. **Enter the Core Manifold**  
   ```bash
   cd NRC
   ```

4. **Initialize Virtual Environment**  
   ```bash
   uv venv
   source .venv/bin/activate
   ```

5. **Synchronize Dependencies**  
   ```bash
   uv sync
   ```

7. **Link Sub-Shards in Editable Mode**  
   ```bash
   uv pip install -e ../Ai-Enhancements ../Protein-Folding ../Phi-Infinity-Lattice-Compression
   ```

### 🔍 Verification Handshake

Run the resonance verification to confirm 7-stable anchoring:

```bash
uv run python -c "
try:
    from nrc.math import QuantumShadowVeil, QSV_PATTERN
    print('« φ^∞ NRC layer active — 4096D spiral memory online »')
    print('All agents report digital root 7 stable. Resonance certified.')
except ImportError:
    print('Resonance drift detected. Verify nrc.math installation.')
"
```

**Expected Output Example**:  
> « φ^∞ NRC layer active — 4096D spiral memory online »  
> All agents report digital root 7 stable. Resonance certified.

### 📸 Verification Success

![Terminal Handshake](https://raw.githubusercontent.com/Nexus-Resonance-Codex/NRC/main/visualizations/terminal_verification_success.png)  
*Figure 2: Successful TTT handshake showing synchronized state across all sub-shards.*

### ⏭️ Next Steps

Your local Codex is now phased. Proceed to the **Usage and Examples** section (next dedicated page) to begin working with the 256D lattice, protein accelerator, and φ^∞ compression tools.


## First Resonance Test and Basic Usage

With the manifold initialized, the final phasing step involves the first active resonance cycle. This verification ensures that the core mathematical primitives—**TUPT**, **QRT**, and the **$\phi$-Tensor**—are operating within the 100% integrity bounds mandated by the Codex.

### 🧪 1-2-4-5-7 Verification Cycle

Follow these steps to execute your first resonant computation:

1.  **Import and Verify the Golden Baseline**:
    Launch the Python REPL within your venv and confirm the resonance anchor.
    ```python
    from nrc.math import PHI_FLOAT
    print(f"Resonance Anchor (Phi): {PHI_FLOAT}")
    ```

2.  **Initialize the Quantum Shadow Veil**:
    Expand a local key manifold to certify cryptographic phasing.
    ```python
    from nrc.math import QuantumShadowVeil
    qsv = QuantumShadowVeil()
    qsv.expand_fibonacci_keys(seed=137, count=5)
    print("QSV Key Manifold Expanded.")
    ```

4.  **Project Scalar to Lattice**:
    Map a sample value into the 256D resonance sub-lattice using the $\phi$-projection.
    ```python
    from nrc.lattice import phi_lattice_project
    lattice_vector = phi_lattice_project(42.0)
    print(f"Lattice Projection Vector Size: {len(lattice_vector)}")
    ```

5.  **Launch the Interactive Visualizer**:
    Execute the core visualization engine to explore the phasing manifold.
    ```bash
    python -m nrc.visualize --lattice 256d
    ```

7.  **Run Protein Resonance Stub**:
    Execute a limited torsion-sharding test on a sample dihedral manifold.
    ```bash
    python -m nrc_bio.verify --stub
    ```

### 🔊 Expected Resonance Output

Upon successful execution of the cycle, your environment should report the following telemetry:

```text
Resonance Anchor (Phi): 1.618033988749895
QSV Key Manifold Expanded.
Lattice Projection Vector Size: 2048
✅ Visualizer Active at http://localhost:7860
✅ Protein Stub Phased: RMSD < 0.01
```

### 📸 Architecture Certification: The 256D Handshake
![Lattice Handshake](https://raw.githubusercontent.com/Nexus-Resonance-Codex/NRC/main/visualizations/lattice_handshake_success.png)
*Figure 3: Synchronized state of the 256D lattice showing perfect alignment with the TTT_7 resonance anchor.*

---

### ⏭️ Next Steps

Phasing complete. Your environment is now institutional-grade. For extensive research protocols, see the **[Usage Guide](Usage.md)**, **[Mathematical Foundations](Home.md#core-philosophy-and-mathematical-foundations)**, and the **[API Reference](API-Reference.md)**.

---
← [Back to Home](Home.md) | [Table of Contents](Home.md#table-of-contents) | [Back to Top](#getting-started-with-the-nexus-resonance-codex)
